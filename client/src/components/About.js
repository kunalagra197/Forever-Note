import React from 'react'
import { useContext } from 'react'
import noteContext from '../context/notes/noteContext'
const About = () => {
  return (
    <div style={{"font-family":"cursive"}}>The Forever Notes APP is a web application built using the MERN stack (MongoDB, ExpressJS, ReactJS, and NodeJS) that allows users to create, read, update, and delete notes. The application provides a simple and intuitive interface for users to organize their notes and quickly access them when needed.</div>
  )
}

export default About